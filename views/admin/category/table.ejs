<table id="example" class="table table-striped" style="width:100%">
    <thead>
        <tr>
            <th style="text-align: left;">No.</th>
            <th>Name</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        <% for (let i = 0; i < category.length; i++) { %>
            <tr>
                <td style="text-align: left;"><%= i + 1 %></td>
                <td><%= category[i].name %></td>
                <td>
                    <form action="/category/delete/<%= category[i]._id %>?_method=DELETE" method="POST">
                        <a href="/category/edit/<%= category[i]._id %>" class="btn btn-sm btn-warning">
                            <i class="bi bi-pencil m-1"></i>Edit</a>
                        <button type="submit" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash m-1"></i>Delete</button>
                    </form>
                </td>
            </tr>
        <% } %>
    </tbody>
</table>